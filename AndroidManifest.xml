<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sssta.joinus"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="11"
        android:targetSdkVersion="15" />
    <!-- 在SDCard中创建与删除文件权限 -->  
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS"/>  
    <!-- 往SDCard写入数据权限 -->  
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>  
<uses-permission android:name="android.permission.INTERNET"></uses-permission>  
    <application
        android:name="joinus_init" 
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
      
         android:theme="@android:style/Theme.NoTitleBar" >
        <activity
            android:name=".welcome"
            android:label="Join Us" >
             <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main" >
           
        </activity>
         <activity
            android:name=".activity_recent"
            android:label="@string/activity_recent" >
            </activity>
          <activity
            android:name=".activity_search"
            android:label="@string/activity_search" >
            </activity>
            <activity
            android:name=".activity_publish"
            android:label="@string/activity_publish" >
            </activity>
             <activity
            android:name=".activity_detail"
            android:label="@string/activity_search" >
            </activity>
            <activity
            android:name=".myActivity"
            android:label="@string/myActivity" >
            </activity>
             <activity
            android:name=".activity_login"
            android:label="@string/myActivity" >
            </activity>
              <activity
            android:name=".activity_club_detail"
            android:label="@string/myActivity" >
            </activity>
            
              <activity
            android:name=".CannotConnectServer"
            android:label="@string/myActivity" >
            </activity>
            <activity
            android:name=".activity_bigpicture"
            android:label="@string/myActivity" >
            </activity>
            
    </application>

</manifest>